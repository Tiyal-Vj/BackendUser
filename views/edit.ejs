<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main min-h-screen w-full bg-zinc-900 p-10 text-white">
      
      <div class="flex justify-end w-full">
        <a href="/logout" class="px-2 py-1 bg-red-600 rounded-md text-sm mb-2 inline-block">Logout</a>
      </div>
      
      <h5 class="mb-4">Edit your post</h5>

      <form action="/update/<%= post._id %>" method="post">
        <textarea class="w-1/3 outline-none block resize-none border-2 border-zinc-800 text-sm p-3 rounded-md bg-transparent"  name="content"  > <%= post.content %> </textarea>
        <input type="submit" class="px-3 py-2 bg-blue-500 rounded-md mt-3">
      </form>

     
    </div>
  </body>
</html>
